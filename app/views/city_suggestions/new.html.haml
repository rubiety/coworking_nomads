%h1 New City Suggestion

= form_for @city_suggestion do |f|
  -if @city_suggestion.errors.any?
    #error_explanation
      %h2= "#{pluralize(@city_suggestion.errors.count, "error")} prohibited this city_suggestion from being saved:"
      %ul
        - @city_suggestion.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name, "City Name"
    = f.text_field :name
  .field
    = f.label :from_name, "Your Name"
    = f.text_field :from_name
  .field
    = f.label :from_email, "Your Email"
    = f.text_field :from_email

  .actions
    = f.submit 'Submit'
