%h1 Listing cities

%table
  %tr
    %th Name
    %th Active
    %th
    %th
    %th

  - @cities.each do |city|
    %tr
      %td= city.name
      %td= city.active
      %td= link_to 'Show', city
      %td= link_to 'Edit', [:edit, city]
      %td= link_to 'Destroy', city, :confirm => 'Are you sure?', :method => :delete

%br

%p
  = link_to 'Create City', new_city_path if can?(:create, City)
